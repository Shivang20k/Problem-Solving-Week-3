#include <iostream>
#include <math.h>
using namespace std;
 
int main() {
  int n  =101;
  int i, sum =0;
  while (n>0){
    int rem = (n&1);
	
    sum = sum + (pow(2,i++)* rem);
	
    n=n/10;
	
  }
  cout<< sum;
}
